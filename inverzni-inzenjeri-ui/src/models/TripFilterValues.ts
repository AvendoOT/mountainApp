export interface TripFilterValues {
    keyWords: string;
    minimalDuration: string;
    minimalDifficulty: string;
    minimalGrade: string;
    maximalDuration: string;
    maximalDifficulty: string;
    maximalGrade: string;
}